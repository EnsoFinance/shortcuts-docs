# Base Tokens

## Summary

Base tokens are normal tokens that do not represent a DeFi position. Base tokens are underlying token of a [DeFi Position Token](./defiTokens.mdx) and can be used to enter a DeFi token/positions.

## Endpoints

You can use the [tokens endpoint](../../metadata-api/api-reference/tokens), filtering by `baseTokens`

